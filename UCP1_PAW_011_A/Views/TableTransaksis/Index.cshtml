@model IEnumerable<UCP1_PAW_011_A.Models.TableTransaksi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdPembeli)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProduk)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdAdmin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalTransaksi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdPembeli)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProduk)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdAdmin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalTransaksi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTransaksi">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTransaksi">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTransaksi">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
